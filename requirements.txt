# Core: chain reads and the position client
web3==7.5.0
eth-abi==5.2.0
python-dotenv==1.0.1

# API
flask==3.0.0
flask-cors==4.0.0

# Testing
pytest==7.4.4
pytest-cov==4.1.0

# NOTE: numpy, pandas, scipy, and scikit-learn are intentionally absent.
# No model has been trained yet; nothing imports them. They return when
# the PD/LGD work begins and are pinned then, against what that work
# actually uses.